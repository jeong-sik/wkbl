name: Refresh WKBL Data

on:
  schedule:
    # 매일 03:00 UTC (한국시간 12:00)
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup OCaml
        uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: 5.2.x

      - name: Cache opam dependencies
        uses: actions/cache@v4
        with:
          path: ~/.opam
          key: ${{ runner.os }}-opam-${{ hashFiles('ocaml/dune-project', 'ocaml/*.opam') }}
          restore-keys: |
            ${{ runner.os }}-opam-

      - name: Install dependencies
        run: |
          cd ocaml
          opam install . --deps-only --yes

      - name: Build scraper tool
        run: |
          cd ocaml
          opam exec -- dune build bin/scraper_tool.exe

      - name: Sync schedule
        env:
          WKBL_DATABASE_URL: ${{ secrets.WKBL_DATABASE_URL }}
        run: |
          cd ocaml
          opam exec -- dune exec bin/scraper_tool.exe -- sync schedule

      - name: Sync boxscores
        env:
          WKBL_DATABASE_URL: ${{ secrets.WKBL_DATABASE_URL }}
        run: |
          cd ocaml
          opam exec -- dune exec bin/scraper_tool.exe -- sync boxscore
